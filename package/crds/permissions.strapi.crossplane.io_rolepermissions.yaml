---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: rolepermissions.permissions.strapi.crossplane.io
spec:
  group: permissions.strapi.crossplane.io
  names:
    categories:
    - crossplane
    - managed
    - strapi
    kind: RolePermissions
    listKind: RolePermissionsList
    plural: rolepermissions
    singular: rolepermissions
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.forProvider.role
      name: ROLE
      type: string
    - jsonPath: .status.atProvider.roleID
      name: ROLE-ID
      type: integer
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: |-
          RolePermissions manages the permission set of a single Strapi
          users-permissions role.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: A RolePermissionsSpec defines the desired state of a RolePermissions.
            properties:
              forProvider:
                description: RolePermissionsParameters are the configurable fields
                  of a RolePermissions.
                properties:
                  permissions:
                    description: |-
                      Permissions is the full set of action strings granted to the role,
                      e.g. "api::article.article.find" or
                      "plugin::users-permissions.auth.callback". Strapi's PUT semantics replace
                      the entire permission set, so this list is authoritative — anything not
                      listed is revoked on the next reconcile.
                    items:
                      type: string
                    type: array
                  role:
                    description: |-
                      Role identifies the users-permissions role to manage.
                      Built-in values are "public" and "authenticated", matched case-insensitively
                      against Strapi's role.type field. Other values are matched against role.name.
                      The role must already exist in Strapi; this resource manages the role's
                      permission set, not its lifecycle.
                    minLength: 1
                    type: string
                required:
                - role
                type: object
              managementPolicies:
                default:
                - '*'
                description: |-
                  THIS IS A BETA FIELD. It is on by default but can be opted out
                  through a Crossplane feature flag.
                  ManagementPolicies specify the array of actions Crossplane is allowed to
                  take on the managed and external resources.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                  and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
                items:
                  description: |-
                    A ManagementAction represents an action that the Crossplane controllers
                    can take on an external resource.
                  enum:
                  - Observe
                  - Create
                  - Update
                  - Delete
                  - LateInitialize
                  - '*'
                  type: string
                type: array
              providerConfigRef:
                default:
                  kind: ClusterProviderConfig
                  name: default
                description: |-
                  ProviderConfigReference specifies how the provider that will be used to
                  create, observe, update, and delete this managed resource should be
                  configured.
                properties:
                  kind:
                    description: Kind of the referenced object.
                    type: string
                  name:
                    description: Name of the referenced object.
                    type: string
                required:
                - kind
                - name
                type: object
              writeConnectionSecretToRef:
                description: |-
                  WriteConnectionSecretToReference specifies the namespace and name of a
                  Secret to which any connection details for this managed resource should
                  be written. Connection details frequently include the endpoint, username,
                  and password required to connect to the managed resource.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                required:
                - name
                type: object
            required:
            - forProvider
            type: object
          status:
            description: A RolePermissionsStatus represents the observed state of
              a RolePermissions.
            properties:
              atProvider:
                description: RolePermissionsObservation are the observable fields
                  of a RolePermissions.
                properties:
                  cleared:
                    description: |-
                      Cleared is set by Delete after wiping the role's permissions in Strapi.
                      On the next deletion reconcile Observe uses it to report the external
                      resource as gone, so the finalizer drains instead of looping on Delete
                      (the role itself is never removed by this MR).
                    type: boolean
                  permissions:
                    description: Permissions is the current permission action set
                      as observed on the role.
                    items:
                      type: string
                    type: array
                  roleID:
                    description: RoleID is the numeric identifier of the resolved
                      role in Strapi.
                    type: integer
                  type:
                    description: Type is the type field of the resolved role (e.g.
                      "public").
                    type: string
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        LastTransitionTime is the last time this condition transitioned from one
                        status to another.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A Message containing details about this condition's last transition from
                        one status to another, if any.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: |-
                        Type of this condition. At most one of each condition type may apply to
                        a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              observedGeneration:
                description: |-
                  ObservedGeneration is the latest metadata.generation
                  which resulted in either a ready state, or stalled due to error
                  it can not recover from without human intervention.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
